const SampleModel = require('../models/sample');

const sampleCode = (req, res) => {
//    setInterval(()=>{
//        console.log('sample');
//    },5000).unref();
};

module.exports = {
    sampleCode
}